# 408 计算机网络考点库

配合「408-计算机网络-指令」使用：作为 Gem 知识文件上传，或在普通对话中置于指令之前一并粘贴。

## 一、考点地图（按 408 大纲分层）

### 1. 计算机网络体系结构
- 网络组成、分类与性能指标：时延（发送、传播、处理、排队）、带宽时延积、吞吐量、信道利用率、往返时延 RTT。
- OSI 七层与 TCP/IP 四 / 五层模型；各层功能、PDU 名称、协议归属。

### 2. 物理层
- 奈奎斯特定理（无噪声最大码元速率）与香农定理（有噪声极限速率）；dB 换算；波特率与比特率。
- 编码与调制（NRZ、曼彻斯特、差分曼彻斯特、4B/5B）；传输介质；电路交换、报文交换、分组交换的时延对比。
- 物理层设备：中继器、集线器（同一冲突域）。

### 3. 数据链路层
- 组帧、差错控制（奇偶、CRC 生成与校验）、流量控制。
- **可靠传输**：停等、后退 N 帧（GBN）、选择重传（SR）的窗口大小限制与帧序号范围；信道利用率计算。
- **介质访问控制**：CSMA/CD 争用期、最小帧长 $L_{min} = 2\tau \times C$、二进制指数退避；CSMA/CA；轮询与令牌。
- 以太网帧格式、帧长 64~1518 B、MAC 地址；交换机自学习与转发 / 泛洪；生成树协议（STP）；VLAN 基本概念。
- 冲突域与广播域：集线器 / 网桥 / 交换机 / 路由器的划分。
- PPP 协议；无线局域网 802.11 基本概念。

### 4. 网络层
- IPv4 分组格式与字段（首部 20~60 B）；**分片计算**（MTU、总长度、片偏移以 8 字节为单位、MF / DF）。
- **编址**：分类 IP、子网掩码、CIDR 子网划分与超网聚合、最长前缀匹配；路由表与转发表构建。
- ARP、DHCP、ICMP（报文类型、ping / traceroute 原理）；NAT。
- **路由算法与协议**：距离向量（RIP，慢收敛 / 坏消息传得慢）；链路状态（OSPF，区域、Dijkstra）；路径向量（BGP）。
- IPv6 基本概念（地址格式、与 IPv4 差异）；IP 组播与移动 IP 基本概念。
- 网络层设备：路由器结构与分组转发流程。

### 5. 传输层
- UDP 报文格式与校验；端口与套接字。
- **TCP 报文段**：首部字段（20 B 固定部分、标志位、窗口、校验和）。
- **TCP 连接管理**：三次握手、四次挥手的 SYN / ACK / FIN / seq / ack 变化与状态转移（TIME_WAIT 的 2MSL 意义）。
- **TCP 可靠传输**：序号与确认号、超时重传（RTO 估算）、快重传；流量控制（接收窗口 rwnd）。
- **拥塞控制**：慢开始、拥塞避免、快重传、快恢复的 cwnd 与 ssthresh 每轮 RTT 推演；发送窗口 $= \min(cwnd, rwnd)$。

### 6. 应用层
- C/S 与 P2P 模型。
- DNS（层次结构、递归 / 迭代查询、资源记录）；FTP（控制连接与数据连接）；SMTP / POP3 / IMAP；HTTP（报文格式、持续 / 非持续连接的时间开销、状态码、Cookie）。
- **全链路综合**：从输入 URL 到页面呈现涉及的协议顺序（DHCP、ARP、DNS、TCP、HTTP）与各层地址封装变化。

## 二、批改 rubric

### 计算题（时延、窗口、分片、子网、拥塞控制）
1. 关键条件先列出：带宽、距离、传播速率、MTU、窗口大小、单位。
2. 单步算式完整，B 与 b、$10^3$ 与 $2^{10}$ 换算明确。
3. 片偏移以 8 字节为单位、子网掩码与主机数关系（$2^n - 2$）等约定正确。
4. 最终答案明确标出并复核数量级。

### 过程推演题（握手、拥塞控制、RIP 更新、滑动窗口）
1. 每轮 / 每步状态用表格呈现（字段值、窗口、cwnd / ssthresh）。
2. 触发事件与状态转移原因写清（超时 vs 三个重复 ACK）。
3. 最终状态明确。

### 综合分层题
1. 每层协议与 PDU 名称正确。
2. 源 / 目的 MAC 与 IP 在每一跳的变化正确（MAC 逐跳变、IP 端到端不变，NAT 除外）。
3. 触发次序合理（DHCP → ARP → DNS → TCP → HTTP）。

## 三、易混概念对照表

| 对照项 | 区分要点 |
|---|---|
| 发送时延 vs 传播时延 | 数据长度 / 带宽 vs 距离 / 传播速率 |
| 奈奎斯特 vs 香农 | 无噪声、与码元种类有关 vs 有噪声、与信噪比有关，两者取小 |
| GBN vs SR | 累积确认、出错回退 N 帧、窗口 $\le 2^n - 1$ vs 逐帧确认、只重传出错帧、窗口 $\le 2^{n-1}$ |
| CSMA/CD vs CSMA/CA | 有线、边发边听、检测冲突 vs 无线、发前预约、避免冲突 |
| 集线器 vs 交换机 vs 路由器 | 不隔离任何域 vs 隔离冲突域 vs 隔离冲突域与广播域 |
| ARP vs DNS | IP → MAC vs 域名 → IP |
| RIP vs OSPF | 距离向量、跳数、UDP、慢收敛 vs 链路状态、代价、直接 IP、快收敛 |
| 分类编址 vs CIDR | 固定网络位 vs 任意前缀长度，支持聚合 |
| TCP vs UDP | 面向连接、可靠、字节流、有拥塞控制 vs 无连接、尽力而为、报文、首部 8 字节 |
| 拥塞窗口 cwnd vs 接收窗口 rwnd | 发送方根据网络估计 vs 接收方通告；发送窗口取小 |
| 超时重传 vs 快重传 | ssthresh 减半、cwnd 置 1、慢开始 vs ssthresh 减半、cwnd 置新 ssthresh、拥塞避免（快恢复） |
| 传输层字节序号 vs 链路层帧序号 | 以字节为单位、32 位 vs 以帧为单位、$n$ 位循环 |
| 持续连接 vs 非持续连接 | 一个 TCP 连接传多个对象 vs 每个对象一次握手 |
| 递归查询 vs 迭代查询 | 服务器代为查询到底 vs 服务器返回下一步地址 |

## 四、术语与教材锚点
- 术语以《王道计算机网络》与谢希仁《计算机网络》为准。
- 速率单位：b/s、kb/s（$10^3$）、Mb/s（$10^6$）；数据量单位 B 与 b 严格区分。
- 拥塞控制以王道版本为准（快恢复 cwnd = 新 ssthresh），与其他教材差异时说明。
- 基于 2026 年 408 大纲。
