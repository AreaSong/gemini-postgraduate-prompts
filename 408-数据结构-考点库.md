# 408 数据结构考点库

配合「408-数据结构-指令」使用：作为 Gem 知识文件上传，或在普通对话中置于指令之前一并粘贴。

## 一、考点地图（按 408 大纲章节）

### 1. 绪论
- 逻辑结构与存储结构；算法的时间 / 空间复杂度分析（渐进记号、递归式求解、常见循环结构的复杂度）。

### 2. 线性表
- 顺序表与单链表的插入 / 删除与复杂度；带头结点 vs 不带头结点；双链表、循环链表、静态链表的指针操作与边界条件。

### 3. 栈、队列和数组
- 栈的应用：表达式求值（中缀 / 后缀转换）、括号匹配、递归转非递归；共享栈。
- 队列：循环队列的判空判满（牺牲一个单元 / 计数器 / 标志位）；双端队列的出入序列合法性。
- 数组：特殊矩阵（对称、三角、三对角）的压缩存储与下标映射；稀疏矩阵三元组。

### 4. 串
- 串的模式匹配：朴素算法；KMP 的 `next` / `nextval` 数组手工推算与失配回退。

### 5. 树与二叉树
- 二叉树性质（结点数、深度、叶子与度）；先序 / 中序 / 后序 / 层序遍历及递归与非递归实现；由遍历序列还原二叉树。
- 线索二叉树；树与森林的存储与遍历、与二叉树的转换。
- 二叉排序树；平衡二叉树（AVL）四种旋转（LL / RR / LR / RL）；红黑树基本性质。
- 哈夫曼树与哈夫曼编码（带权路径长度）；并查集及路径压缩。

### 6. 图
- 存储：邻接矩阵、邻接表、十字链表、邻接多重表。
- 遍历：DFS / BFS 及序列合法性；连通分量。
- 最小生成树 Prim / Kruskal；最短路径 Dijkstra / Floyd / BFS（无权图）；拓扑排序；关键路径（AOE 网、事件最早 / 最迟发生时间、活动余量）。

### 7. 查找
- 顺序查找、折半查找（判定树、ASL）；分块查找。
- B 树与 B+ 树的插入、删除、分裂与合并；阶数与关键字数的关系。
- 散列：散列函数构造；冲突处理（开放定址：线性探测 / 平方探测；拉链法）；装填因子与 ASL 计算。

### 8. 排序
- 插入类（直接插入、折半插入、希尔）、交换类（冒泡、快速）、选择类（简单选择、堆）、归并、基数排序。
- 稳定性、时间 / 空间复杂度、各趟结果、比较次数与移动次数；适用场景。
- 外部排序：多路归并、败者树、置换-选择排序、最佳归并树。

## 二、批改 rubric

### 算法设计大题（三段式）
1. **设计思想**：文字描述清晰，说明核心数据结构与遍历 / 处理顺序。
2. **代码**：标准 C 风格；结构体定义完整；边界处理（空表、单结点、越界）；核心行有注释。
3. **复杂度**：时间与空间复杂度准确并给出理由。
4. 常见扣分：只给代码无思想；未处理边界；复杂度分析错误；用高级语言语法糖。

### 手算推演题（遍历、排序趟数、树的构造、图算法）
1. 每步状态用表格或图示呈现，可回溯。
2. 中间结果与最终结果明确。
3. 标注该题型高频陷阱（如快排枢轴选择、堆排序建堆顺序、Dijkstra 不适用负权）。

## 三、易混概念对照表

| 对照项 | 区分要点 |
|---|---|
| 满二叉树 vs 完全二叉树 | 每层都满 vs 仅最后一层从左到右连续缺失 |
| 深度 vs 高度 | 从根向下数 vs 从叶向上数；起始值教材有 0/1 差异 |
| 二叉排序树 vs 平衡二叉树 | 有序性 vs 有序性 + 平衡因子绝对值 ≤ 1 |
| B 树 vs B+ 树 | 关键字在所有结点 vs 仅叶结点含全部关键字且链接；B+ 更适合范围查询 |
| Prim vs Kruskal | 加点、适合稠密图 $O(V^2)$ vs 加边、适合稀疏图 $O(E \log E)$ |
| Dijkstra vs Floyd | 单源、不支持负权 vs 多源、支持负权但不支持负环 |
| 拓扑排序 vs 关键路径 | 判断 DAG 与排序 vs 求最长路径与关键活动 |
| 稳定排序 vs 不稳定排序 | 稳定：插入、冒泡、归并、基数；不稳定：快速、简单选择、堆、希尔 |
| 快速排序 vs 归并排序 | 原地、平均 $O(n\log n)$ 最坏 $O(n^2)$ vs 需 $O(n)$ 辅助空间、稳定 $O(n\log n)$ |
| 开放定址 vs 拉链法 | 删除需标记、易堆积 vs 可直接删除、需指针空间 |
| 邻接矩阵 vs 邻接表 | 空间 $O(V^2)$、查边 $O(1)$ vs 空间 $O(V+E)$、查边 $O(\deg)$ |
| 循环队列判空 vs 判满 | 牺牲一单元时 `front == rear` vs `(rear+1)%N == front` |

## 四、术语与教材锚点
- 术语以《王道数据结构》与严蔚敏教材为准；「结点」统一写法。
- 深度 / 高度起始值、树的度等定义按王道；与其他教材差异时说明。
- 代码风格：`ElemType`、`LinkList`、`BiTree` 等王道常用类型名可直接使用。
