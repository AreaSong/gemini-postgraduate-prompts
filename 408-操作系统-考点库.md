# 408 操作系统考点库

配合「408-操作系统-指令」使用：作为 Gem 知识文件上传，或在普通对话中置于指令之前一并粘贴。

## 一、考点地图（按 408 大纲五章）

### 1. 操作系统概述
- 概念、特征（并发、共享、虚拟、异步）与功能；发展历程（批处理、分时、实时）。
- 运行环境：内核态与用户态切换；中断与异常分类（内中断/外中断、陷入/故障/终止）；系统调用的执行过程与库函数的区别；程序的链接与装入；运行时内存映像与地址空间。
- 体系结构：分层、模块化、宏内核 vs 微内核 vs 外核；操作系统引导过程；虚拟机（第一类/第二类）。

### 2. 进程管理
- **进程与线程**：进程状态转换（五状态/七状态）、PCB、进程控制原语；线程实现（用户级/内核级）、多线程模型（一对一、多对一、多对多）；进程间通信（共享内存、消息传递、管道、信号）；上下文切换。
- **处理机调度**：调度层次与时机；抢占 / 非抢占；FCFS、SJF、优先级、RR、多级反馈队列、HRRN；周转时间、带权周转时间、响应比的计算与甘特图推演；多处理机调度。
- **同步与互斥**：临界区四原则；软件方法（Peterson）、硬件方法（关中断、TestAndSet、Swap）；锁、信号量 PV、条件变量；经典模型（生产者-消费者、读者-写者、哲学家就餐、理发师）及其变形；管程。
- **死锁**：四个必要条件；预防（破坏条件）、避免（银行家算法：Max、Allocation、Need、Available 矩阵与安全序列）、检测（资源分配图化简）、解除。

### 3. 内存管理
- **连续分配**：单一连续、固定分区、动态分区（首次适应、最佳适应、最坏适应、邻近适应）；内部碎片 vs 外部碎片。
- **非连续分配**：基本分页（页号 P、页内偏移 W、页表项）、基本分段、段页式的地址结构与转换。
- **地址转换综合链路**：TLB 命中/未命中、多级页表、缺页中断、Cache 的全流程访存次数与有效访问时间计算。
- **虚拟内存**：请求分页；缺页中断处理流程；页面置换算法（OPT、FIFO 及 Belady 异常、LRU、CLOCK、改进型 CLOCK）的缺页次数表格推演；页面分配策略（固定/可变分配、局部/全局置换）；工作集与抖动；内存映射文件。

### 4. 文件管理
- **文件**：逻辑结构（顺序、索引、索引顺序、散列）；文件操作与打开文件表（系统级/进程级）；文件保护（访问控制列表、权限位）。
- **目录**：FCB 与索引结点（inode）；单级/两级/树形/无环图目录；文件共享（硬链接 vs 软链接）。
- **文件系统实现**：分配方式（连续、链接、索引）；多级索引文件大小计算（直接、一级/二级/三级间接寻址）；外存空闲空间管理（空闲表、空闲链表、位示图计算、成组链接法）。
- **文件系统全局结构**：磁盘分区与挂载；虚拟文件系统（VFS）。

### 5. I/O 管理
- **I/O 控制方式**：轮询、中断驱动、DMA 的 CPU 干预程度对比（大纲三种；通道若出现先标超纲）。
- **I/O 软件层次**：用户层、设备无关层、设备驱动、中断处理程序；设备接口（字符 / 块 / 网络）、阻塞 vs 非阻塞 I/O。
- **应用**：单缓冲/双缓冲的处理时间计算；SPOOLing 技术；设备分配与回收。
- **磁盘**：结构与访问时间三段式（寻道 + 旋转延迟 + 传输）；磁盘调度算法（FCFS、SSTF、SCAN、C-SCAN、LOOK、C-LOOK）的寻道距离与磁头轨迹推演；格式化与引导块、坏块管理。
- **固态硬盘**：闪存翻译层（FTL）、磨损均衡、读写不对称与擦除特性。

## 二、大题批改 rubric

### PV 操作题
1. 信号量定义齐全，**初始值**正确。
2. 互斥信号量**紧贴临界区**，不包裹无关代码。
3. 多个 P 嵌套时，**同步 P 在互斥 P 之前**。
4. 无死锁、无饥饿；无假死循环。
5. 关键行有注释，说明该 P/V 对应的资源或条件。

### 计算题（置换、调度、磁盘、地址转换、缓冲）
1. 逐步状态用表格呈现，每一步可回溯。
2. 单位齐全（B / KB / ns / 次）。
3. 最终答案明确标出。
4. 标注该题型的高频易错点（如 LRU 与 FIFO 的淘汰顺序、页内偏移位数、SCAN 初始磁头方向、缓冲区最后一块的处理）。

## 三、易混概念对照表

| 对照项 | 区分要点 |
|---|---|
| 库函数 vs 系统调用 | 库函数在用户态，可能封装系统调用；系统调用必陷入内核态 |
| 中断 vs 异常 | 中断来自 CPU 外部、异步；异常由当前指令引发、同步 |
| 进程 vs 线程 | 资源分配单位 vs 调度单位；线程共享进程地址空间 |
| 用户级线程 vs 内核级线程 | 切换是否需要陷入内核；一个阻塞是否影响同进程其他线程 |
| 共享内存 vs 消息传递 vs 管道 | 共写一段内存、最快 vs 内核转发消息 vs 字节流；管道常用于亲缘进程 |
| 固定分配 vs 可变分配 | 进程驻留页数是否可变 |
| 局部置换 vs 全局置换 | 淘汰页是否只限于本进程 |
| 抖动 vs 正常缺页 | 抖动指频繁换入换出导致 CPU 利用率骤降，与工作集不足相关 |
| SCAN vs LOOK | SCAN 走到磁盘端点才折返；LOOK 到最远请求即折返 |
| 硬链接 vs 软链接 | 硬链接共享 inode、计数增加；软链接是独立文件存路径 |
| 内部碎片 vs 外部碎片 | 已分配块内未用 vs 分配块之间无法利用的空隙 |
| 宏内核 vs 微内核 vs 外核 | 内核功能多、性能高 vs 内核精简、可靠性高但切换开销大 vs 内核只保留资源复用与保护，策略在用户态 |

## 四、术语与教材锚点
- 术语以《王道操作系统》为准；「节点 / 结点」统一写为「结点」。
- `P()` / `V()` 与 `wait()` / `signal()` 等价，批改时按用户所用写法保持一致。
- 页面置换、磁盘调度算法名称同时给中文与英文缩写。
- 基于 2026 年 408 大纲。
