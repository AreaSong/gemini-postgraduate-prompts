# 408 计算机组成原理考点库

配合「408-计算机组成原理-指令」使用：作为 Gem 知识文件上传，或在普通对话中置于指令之前一并粘贴。

## 一、考点地图（按 408 大纲章节）

### 1. 计算机系统概述
- 冯·诺依曼结构与层次结构；CPU、主存、I/O 的基本组成与工作过程。
- 性能指标：字长、主频、CPI、MIPS、MFLOPS、吞吐量、响应时间；Amdahl 定律。

### 2. 数据的表示和运算
- 进位计数制；BCD 码；字符与字符串；校验码（奇偶、海明、CRC）。
- 定点数：原码、补码、反码、移码的转换；符号扩展；溢出判断（双符号位、进位法）；定点加减乘除运算与 ALU。
- 浮点数：IEEE 754 单精度（1 + 8 + 23）与双精度格式；规格化；隐含 1；阶码偏置；浮点加减运算（对阶、尾数运算、规格化、舍入、溢出判断）。
- C 语言中的类型转换与大端 / 小端存储；边界对齐。

### 3. 存储系统
- 存储器层次结构；SRAM / DRAM / ROM / Flash 特性；DRAM 刷新与地址复用。
- 主存扩展：位扩展、字扩展、字位同时扩展的芯片选片逻辑与地址范围计算。
- 多模块存储器：高位交叉 vs 低位交叉的访存时间。
- **Cache**：映射方式（直接、全相联、组相联）的主存地址划分（Tag / Index / Offset）；替换算法（LRU、FIFO、随机）；写策略（写直达 / 写回、写分配 / 非写分配）；命中率、缺失率、平均访问时间计算；Cache 总容量含标记位与有效位。
- **虚拟存储器**：页式、段式、段页式；页表与 TLB；结合 Cache 的地址转换全链路。
- **外存储器**：磁盘存储器（结构、访问时间、RAID）与固态硬盘。

### 4. 指令系统
- 指令格式：操作码扩展、地址码个数、定长 / 变长指令。
- 寻址方式：立即、直接、间接、寄存器、寄存器间接、基址、变址、相对、堆栈寻址；有效地址 EA 计算。
- CISC vs RISC；编译器 / 汇编器 / 链接器；高级语言与机器级代码的对应（选择 / 循环结构、过程调用与栈帧）。

### 5. 中央处理器
- CPU 功能与结构；寄存器组（PC、IR、MAR、MDR、ACC、PSW 等）。
- 指令执行过程：取指、间址、执行、中断周期；数据通路（单总线 / 多总线）与微操作序列。
- 控制器：硬布线 vs 微程序；微指令格式与编码方式。
- 异常与中断机制：内部异常与外部中断的处理流程。
- **指令流水线**：基本概念；时空图；吞吐率、加速比、效率；数据冒险（RAW / WAR / WAW）、控制冒险、结构冒险及处理（旁路转发、插入气泡、分支预测）；超标量与动态流水线基本概念。
- 多处理器基本概念：SISD / SIMD / MIMD、向量处理器、多核、SMP（共享内存）、硬件多线程。

### 6. 总线
- 总线分类与结构；总线性能指标（宽度、带宽、时钟频率）与带宽计算。
- 总线仲裁与事务；同步 / 异步 / 半同步 / 分离式定时。

### 7. 输入 / 输出系统
- I/O 接口功能与结构；I/O 端口编址（统一 / 独立）。
- I/O 方式：程序查询、程序中断、DMA。
- **中断**：中断请求、判优、响应（中断隐指令完成的动作 vs 中断服务程序的动作）、多重中断与中断屏蔽字。
- **DMA**：DMA 控制器组成；传送方式（停止 CPU、周期挪用、交替访问）；DMA 与中断的区别；传输速率与 CPU 占用率计算。

## 二、批改 rubric

### 计算题（地址划分、Cache、浮点、流水线、DMA）
1. 关键条件先列出：编址单位、位数、容量、周期。
2. 单步算式完整，单位不省略，2 的幂与 10 的幂换算明确。
3. 地址字段划分表格化，各字段位数之和等于地址总位数。
4. 最终答案明确标出并复核数量级。
5. 易错点标注：按字 / 按字节编址混淆、Cache 容量遗漏标记位、隐含 1 遗漏、流水线首条指令周期数、DMA 周期挪用次数。

### 数据通路 / 控制信号题
1. 每个微操作写清源与目的寄存器。
2. 控制信号命名规范（如 PCout、MARin、MemR）。
3. 时钟周期数与并行可能性说明。

## 三、易混概念对照表

| 对照项 | 区分要点 |
|---|---|
| 原码 vs 补码表示范围 | 原码有 ±0，范围对称；补码多一个负数 $-2^{n-1}$ |
| 双符号位法 vs 进位法判溢出 | 符号位 01 / 10 为溢出 vs 最高位进位与次高位进位异或为 1 |
| 逻辑移位 vs 算术移位 | 补 0 vs 保持符号位、补码右移补符号位 |
| 直接映射 vs 组相联 | Index 定唯一行 vs Index 定组、组内比较 Tag；全相联无 Index |
| 写直达 vs 写回 | 每次写都写主存 vs 替换时才写回、需脏位 |
| 高位交叉 vs 低位交叉 | 顺序编址、串行访问 vs 交叉编址、流水并行 |
| 基址寻址 vs 变址寻址 | 基址寄存器内容不变、偏移在指令中（程序重定位）vs 变址寄存器变、基地址在指令中（数组遍历） |
| 硬布线 vs 微程序控制器 | 速度快、难修改 vs 灵活、慢一个数量级 |
| 中断隐指令 vs 中断服务程序 | 硬件自动：关中断、保存断点、引出服务程序 vs 软件：保存现场、处理、恢复现场、开中断、返回 |
| 中断方式 vs DMA 方式 | 程序切换、以字为单位、CPU 参与传送 vs 硬件控制、以块为单位、仅在开始与结束干预 |
| 同步总线 vs 异步总线 | 统一时钟、速度受限于最慢设备 vs 握手信号、灵活但开销大 |
| 吞吐率 vs 加速比 | 单位时间完成任务数 vs 非流水时间 / 流水时间 |

## 四、术语与教材锚点
- 术语以《王道计算机组成原理》为准；寄存器缩写用 PC、IR、MAR、MDR、ACC、PSW。
- 容量单位：K = $2^{10}$、M = $2^{20}$、G = $2^{30}$（存储容量）；速率单位按题目约定区分 $10^3$ 与 $2^{10}$。
- 流水线周期数、Cache 行大小等题目未给时，先声明假设再计算。
- 基于 2026 年 408 大纲。
